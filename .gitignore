# RStudio and R session
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj.user
.DS_Store

# RStudio history export and stray result dumps
History.rtf
results.rtf

# Cached data, regenerated by data_sources.R then cohort.R
data/

# Quarto build artifacts. report.html is self-contained via embed-resources,
# so it is tracked, but the libs folder and cache are not.
.quarto/
report_files/

# Session scratch and redundant anchor
.RDataTmp
.Rapp.history
.here
